export default function Terminal() {
    return (
        <div
            className="h-full p-3 font-mono text-sm text-[#8b949e] overflow-auto"
            data-testid="terminal"
        >
            <span className="text-[#484f58]">Terminal — boot a sandbox to begin</span>
        </div>
    )
}
